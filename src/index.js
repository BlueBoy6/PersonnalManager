import React from "react";
import ReactDOM from "react-dom";
import RouterApp from "./router/routerApp";
const rootElement = document.getElementById("root");
ReactDOM.render(<RouterApp />, rootElement);
